apiVersion: v1
kind: Service
metadata:
  name: {{ include "immortal.fullname" . }}
  labels:
    {{- include "immortal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "immortal.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
